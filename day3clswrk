SELECT DISTINCT category
FROM products;
SELECT *
FROM product
WHERE stock = 'Yes' AND price < 500;
SELECT *
FROM product
WHERE stock = 'No' OR price > 1000;
SELECT name, price
FROM product
ORDER BY price DESC;
SELECT 
    name, 
    price * 1.18 AS price_with_tax
FROM product;
